# Minimal requirements for Streamlit app deployment
# This is optimized for Streamlit Cloud deployment

# Core dependencies
streamlit>=1.28.0
pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0

# Data handling
pyyaml>=6.0

# Note: Pose estimation libraries (MediaPipe/MMPose) are NOT included
# The app only displays pre-processed videos, no pose estimation at runtime

